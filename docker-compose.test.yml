version: '3.7'
services:
    cht-postgres-test:
        build:
            context: .
            target: test-buld
        container_name: cht_postgres_test
        environment:
            POSTGRES_DB: cht
            POSTGRES_USER: cht
            POSTGRES_PASSWORD: cht_password
            COUCH2PG_USER: cht_couch2pg
            COUCH2PG_USER_PASSWORD: couch2pg_password
            DB_OWNER_GROUP: cht_analytics
        volumes:
            - cht-postgres-test:/var/lib/postgresql/data
    sut:

        container_name: sut
        image: ubuntu:18.04
        environment:
            POSTGRES_DB: cht
            POSTGRES_USER: cht
            POSTGRES_PASSWORD: cht_password
            COUCH2PG_USER: cht_couch2pg
            COUCH2PG_USER_PASSWORD: couch2pg_password
            DB_OWNER_GROUP: cht_analytics
            POSTGRES_HOST:
            POSTGRES_HOST: cht_postgres_test
        volumes:
            - ./:/tests
        command: [ "/bin/sh", "-c", "/tests/run_tests.sh"]


volumes:
    cht-postgres-test:
        name: cht-postgres-test